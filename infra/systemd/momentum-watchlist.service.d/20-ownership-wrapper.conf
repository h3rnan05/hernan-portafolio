# PAPER ONLY. El wrapper vive fuera del árbol porque hace `git pull`
# sobre el árbol (ver infra/systemd/bin/run_momentum_paper.sh).
# 2026-09-15: mismo wrapper que momentum-scan.service, en modo
# watchlist. El VPS es el único escritor de estado en git.
[Service]
ExecStart=
ExecStart=/opt/momentum/bin/run_momentum_paper.sh watchlist
