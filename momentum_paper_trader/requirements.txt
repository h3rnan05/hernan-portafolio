# Dependencias del paper trader -- mismo principio que el resto del
# repo: sin pandas/numpy, corre ligero. `requests` es para las llamadas
# HTTP a la API REST de Alpaca; `anthropic` es para la capa de decisión
# con IA (ver `ia_decision.py`) -- mismo SDK que ya usa `telegram_bot/
# idea_evaluator.py` en el resto del repo.
requests>=2.31.0
anthropic>=0.40.0
