# Dependencias del paper trader -- mismo principio que el resto del
# repo: sin pandas/numpy, corre ligero. `requests` es la única
# dependencia real (llamadas HTTP a la API REST de Alpaca).
requests>=2.31.0
